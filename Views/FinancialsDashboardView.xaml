<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoTable.Views.FinancialsDashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource SurfaceGrayBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="20">

            <!-- KPI CARDS ROW -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="10,0,0,10" Background="{StaticResource SuccessGreenBrush}" />
                        <StackPanel Grid.Column="1" Padding="14,14" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8C7;" FontSize="15" Foreground="{StaticResource SuccessGreenBrush}" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Total Collected" />
                            </StackPanel>
                            <TextBlock FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                       Text="{Binding TotalCollected, Converter={StaticResource CurrencyConverter}}" />
                            <TextBlock Style="{StaticResource MutedTextStyle}" Text="This term" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="10,0,0,10" Background="{StaticResource WarningAmberBrush}" />
                        <StackPanel Grid.Column="1" Padding="14,14" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE783;" FontSize="15" Foreground="{StaticResource WarningAmberBrush}" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Outstanding Fees" />
                            </StackPanel>
                            <TextBlock FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                       Text="{Binding OutstandingFees, Converter={StaticResource CurrencyConverter}}" />
                            <TextBlock Style="{StaticResource MutedTextStyle}" Text="Unpaid balance" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="10,0,0,10" Background="{StaticResource PrimaryBlueBrush}" />
                        <StackPanel Grid.Column="1" Padding="14,14" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8B7;" FontSize="15" Foreground="{StaticResource PrimaryBlueBrush}" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Budget Used" />
                            </StackPanel>
                            <TextBlock FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                       Text="{Binding BudgetUsedPercent, Converter={StaticResource PercentConverter}}" />
                            <TextBlock Style="{StaticResource MutedTextStyle}" Text="Of annual budget" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" CornerRadius="10,0,0,10" Background="{StaticResource AccentPurpleBrush}" />
                        <StackPanel Grid.Column="1" Padding="14,14" Spacing="4">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE77B;" FontSize="15" Foreground="{StaticResource AccentPurpleBrush}" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Students Paid" />
                            </StackPanel>
                            <TextBlock FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                       Text="{Binding StudentsPaidPercent, Converter={StaticResource PercentConverter}}" />
                            <TextBlock Style="{StaticResource MutedTextStyle}" Text="Full payment" />
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- CHARTS ROW -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Fee Collection by Class -->
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <Grid>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Fee Collection by Class" />
                            <TextBlock HorizontalAlignment="Right" Style="{StaticResource MutedTextStyle}"
                                       Text="Term 2, 2025" VerticalAlignment="Center" />
                        </Grid>
                        <Border Height="180" Background="{StaticResource SurfaceGrayBrush}" CornerRadius="8">
                            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Left"
                                        Orientation="Horizontal" Spacing="8" Padding="16,16">
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="120" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="40"  Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P1" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="100" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="55"  Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P2" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="90" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="65" Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P3" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="130" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="25"  Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P4" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="110" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="45"  Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P5" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="80" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="70" Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P6" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Bottom">
                                        <Border Width="28" Height="140" Background="{StaticResource SuccessGreenBrush}" CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.85" />
                                        <Border Width="28" Height="15"  Background="{StaticResource DangerRedBrush}"   CornerRadius="4" VerticalAlignment="Bottom" Opacity="0.7" />
                                    </StackPanel>
                                    <TextBlock Text="P7" FontSize="11" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Border Width="12" Height="12" Background="{StaticResource SuccessGreenBrush}" CornerRadius="2" VerticalAlignment="Center" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Collected" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Border Width="12" Height="12" Background="{StaticResource DangerRedBrush}" CornerRadius="2" VerticalAlignment="Center" />
                                <TextBlock Style="{StaticResource MutedTextStyle}" Text="Outstanding" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Budget Breakdown -->
                <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Budget Breakdown" />
                        <Border Height="180" Background="{StaticResource SurfaceGrayBrush}" CornerRadius="8">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                                <FontIcon Glyph="&#xE9D2;" FontSize="36" Foreground="{StaticResource PrimaryBlueBrush}" />
                                <TextBlock HorizontalAlignment="Center" Style="{StaticResource MutedTextStyle}" Text="Budget chart" />
                            </StackPanel>
                        </Border>
                        <StackPanel Spacing="6">
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" Background="{StaticResource PrimaryBlueBrush}" CornerRadius="2" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Style="{StaticResource MutedTextStyle}" Text="Salaries" />
                                <TextBlock Grid.Column="2" Style="{StaticResource MutedTextStyle}" Text="62%" />
                            </Grid>
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" Background="{StaticResource SuccessGreenBrush}" CornerRadius="2" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Style="{StaticResource MutedTextStyle}" Text="Operations" />
                                <TextBlock Grid.Column="2" Style="{StaticResource MutedTextStyle}" Text="24%" />
                            </Grid>
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <Border Width="10" Height="10" Background="{StaticResource WarningAmberBrush}" CornerRadius="2" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1" Style="{StaticResource MutedTextStyle}" Text="Infrastructure" />
                                <TextBlock Grid.Column="2" Style="{StaticResource MutedTextStyle}" Text="14%" />
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- RECENT TRANSACTIONS -->
            <Border Style="{StaticResource TableCardStyle}">
                <StackPanel>
                    <Border Style="{StaticResource CardHeaderBorderStyle}">
                        <Grid>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Recent Transactions" VerticalAlignment="Center" />
                            <HyperlinkButton HorizontalAlignment="Right" Content="View all →"
                                             FontSize="13" Foreground="{StaticResource PrimaryBlueBrush}" />
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource TableHeaderRowStyle}">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Style="{StaticResource TableHeaderStyle}" Text="STUDENT" />
                            <TextBlock Grid.Column="1" Style="{StaticResource TableHeaderStyle}" Text="CLASS" />
                            <TextBlock Grid.Column="2" Style="{StaticResource TableHeaderStyle}" Text="TERM" />
                            <TextBlock Grid.Column="3" Style="{StaticResource TableHeaderStyle}" Text="AMOUNT" />
                            <TextBlock Grid.Column="4" Style="{StaticResource TableHeaderStyle}" Text="DATE" />
                            <TextBlock Grid.Column="5" Style="{StaticResource TableHeaderStyle}" Text="STATUS" />
                        </Grid>
                    </Border>
                    <ListView ItemsSource="{Binding RecentTransactions}" SelectionMode="None">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="MinHeight" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnSpacing="12" Padding="16,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="90" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding StudentName}" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource TableCellStyle}" Text="{Binding ClassName}" />
                                    <TextBlock Grid.Column="2" Style="{StaticResource TableCellStyle}" Text="{Binding Term}" />
                                    <TextBlock Grid.Column="3" FontSize="14" FontWeight="Bold"
                                               Foreground="{StaticResource SuccessGreenBrush}" VerticalAlignment="Center"
                                               Text="{Binding Amount, Converter={StaticResource CurrencyConverter}}" />
                                    <TextBlock Grid.Column="4" Style="{StaticResource TableCellStyle}" Text="{Binding PaymentDate}" />
                                    <TextBlock Grid.Column="5" Style="{StaticResource TableCellStyle}" Text="{Binding Status}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>

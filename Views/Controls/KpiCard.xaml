<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AutoTable.Controls.KpiCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="260"
    MinHeight="110">

    <Border
        Background="{ThemeResource SurfaceWhiteBrush}"
        BorderBrush="{ThemeResource BorderLightBrush}"
        BorderThickness="1"
        CornerRadius="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Accent left bar -->
            <Border
                x:Name="AccentBar"
                Grid.Column="0"
                CornerRadius="10,0,0,10"
                Background="{ThemeResource PrimaryBlueBrush}" />

            <!-- Content -->
            <StackPanel Grid.Column="1" Padding="18,16,18,16" Spacing="6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon
                        x:Name="IconBlock"
                        FontSize="15"
                        Foreground="{ThemeResource PrimaryBlueBrush}" />
                    <TextBlock
                        x:Name="TitleBlock"
                        Style="{ThemeResource MutedTextStyle}"
                        TextWrapping="Wrap"
                        MaxWidth="120" />
                </StackPanel>
                <TextBlock
                    x:Name="ValueBlock"
                    FontSize="26"
                    FontWeight="Bold"
                    Foreground="{ThemeResource TextPrimaryBrush}" />
                <TextBlock
                    x:Name="SubtitleBlock"
                    Style="{ThemeResource MutedTextStyle}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>


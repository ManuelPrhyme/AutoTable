<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoTable.Views.BudgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource SurfaceGrayBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16">

            <Border Style="{StaticResource FilterBarStyle}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" Header="Financial Year" MinWidth="160"
                              ItemsSource="{Binding FinancialYears}"
                              SelectedItem="{Binding SelectedYear, Mode=TwoWay}" />
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                        <Button Content="Export" Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding ExportCommand}" />
                        <Button Content="Add Line Item" Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding AddLineItemCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Total Budget" />
                        <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                                   Text="{Binding TotalBudget, Converter={StaticResource CurrencyConverter}}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Spent" />
                        <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource DangerRedBrush}"
                                   Text="{Binding TotalSpent, Converter={StaticResource CurrencyConverter}}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Remaining" />
                        <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessGreenBrush}"
                                   Text="{Binding Remaining, Converter={StaticResource CurrencyConverter}}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="180">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Utilisation" />
                        <TextBlock FontSize="20" FontWeight="Bold" Foreground="{StaticResource PrimaryBlueBrush}"
                                   Text="{Binding UtilisationPercent, Converter={StaticResource PercentConverter}}" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Style="{StaticResource TableCardStyle}">
                <StackPanel>
                    <Border Style="{StaticResource CardHeaderBorderStyle}">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Budget Line Items" />
                    </Border>
                    <Border Style="{StaticResource TableHeaderRowStyle}">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Style="{StaticResource TableHeaderStyle}" Text="CATEGORY" />
                            <TextBlock Grid.Column="1" Style="{StaticResource TableHeaderStyle}" Text="BUDGETED" />
                            <TextBlock Grid.Column="2" Style="{StaticResource TableHeaderStyle}" Text="SPENT" />
                            <TextBlock Grid.Column="3" Style="{StaticResource TableHeaderStyle}" Text="REMAINING" />
                            <TextBlock Grid.Column="4" Style="{StaticResource TableHeaderStyle}" Text="UTILISATION" />
                            <TextBlock Grid.Column="5" Style="{StaticResource TableHeaderStyle}" Text="STATUS" />
                        </Grid>
                    </Border>
                    <ListView ItemsSource="{Binding BudgetLines}" SelectionMode="None">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="MinHeight" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnSpacing="12" Padding="16,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding Category}" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource NumericCellStyle}"
                                               Text="{Binding Budgeted, Converter={StaticResource CurrencyConverter}}" />
                                    <TextBlock Grid.Column="2" FontSize="14" FontWeight="Bold"
                                               Foreground="{StaticResource DangerRedBrush}" VerticalAlignment="Center"
                                               Text="{Binding Spent, Converter={StaticResource CurrencyConverter}}" />
                                    <TextBlock Grid.Column="3" FontSize="14" FontWeight="Bold"
                                               Foreground="{StaticResource SuccessGreenBrush}" VerticalAlignment="Center"
                                               Text="{Binding Remaining, Converter={StaticResource CurrencyConverter}}" />
                                    <StackPanel Grid.Column="4" Spacing="3" VerticalAlignment="Center">
                                        <ProgressBar Value="{Binding UtilisationPercent}" Maximum="100"
                                                     Height="6" CornerRadius="3"
                                                     Background="{StaticResource SurfaceGray2Brush}" />
                                        <TextBlock Text="{Binding UtilisationPercent, Converter={StaticResource PercentConverter}}"
                                                   FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="5" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding Status}" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>

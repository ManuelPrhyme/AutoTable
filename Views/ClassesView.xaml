<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoTable.Views.ClassesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:AutoTable.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SurfaceGrayBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16">

            <!-- Header -->
            <StackPanel>
                <TextBlock Style="{ThemeResource PageTitleStyle}" Text="Class Management" />
                <TextBlock Style="{ThemeResource MutedTextStyle}" Text="Create, view and manage all classes in your school." />
            </StackPanel>

            <!-- KPI row -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Style="{ThemeResource KpiCardStyle}" MinWidth="150">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{ThemeResource MutedTextStyle}" Text="Total Classes" />
                        <TextBlock Style="{ThemeResource KpiValueStyle}" Text="{Binding Classes.Count}" />
                    </StackPanel>
                </Border>
                <Border Style="{ThemeResource KpiCardStyle}" MinWidth="150">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{ThemeResource MutedTextStyle}" Text="Total Subjects" />
                        <TextBlock Style="{ThemeResource KpiValueStyle}" Text="{Binding AllSubjects.Count}" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Main grid: left list, right details -->
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left: Classes list and create subject -->
                <Border Grid.Column="0" Style="{ThemeResource TableCardStyle}">
                    <StackPanel>
                        <Border Style="{ThemeResource CardHeaderBorderStyle}">
                            <Grid>
                                <TextBlock Style="{ThemeResource SectionTitleStyle}" Text="All Classes" VerticalAlignment="Center" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                                    <Button Content="Export" Style="{ThemeResource SecondaryButtonStyle}" />
                                    <Button Content="+ Create Class" Style="{ThemeResource PrimaryButtonStyle}" Click="CreateClass_Click" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{ThemeResource TableHeaderRowStyle}">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36" />
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="80" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Style="{ThemeResource TableHeaderStyle}" Text="#" />
                                <TextBlock Grid.Column="1" Style="{ThemeResource TableHeaderStyle}" Text="CLASS NAME" />
                                <TextBlock Grid.Column="2" Style="{ThemeResource TableHeaderStyle}" Text="STREAM" />
                                <TextBlock Grid.Column="3" Style="{ThemeResource TableHeaderStyle}" Text="CLASS TEACHER" />
                                <TextBlock Grid.Column="4" Style="{ThemeResource TableHeaderStyle}" Text="STUDENTS" />
                            </Grid>
                        </Border>

                        <ListView x:Name="ClassesList" ItemsSource="{Binding ClassInfos}" IsItemClickEnabled="True" ItemClick="ClassesList_ItemClick" SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:ClassInfo" xmlns:models="using:AutoTable.Models">
                                    <Grid Padding="12" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="80" />
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Grid.Column="0" Width="28" Height="28" Fill="{ThemeResource PrimaryBlueBrush}" />
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                            <TextBlock Text="{x:Bind SubjectsCsv}" Style="{ThemeResource MutedTextStyle}" FontSize="12" TextWrapping="WrapWholeWords" />
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{x:Bind StreamsCsv}" VerticalAlignment="Center" TextWrapping="WrapWholeWords" />
                                        <TextBlock Grid.Column="3" Text="{x:Bind ClassTeacherName}" VerticalAlignment="Center" TextWrapping="WrapWholeWords"
                                                   Foreground="{ThemeResource TextSecondaryBrush}" />
                                        <TextBlock Grid.Column="4" Text="{x:Bind StudentCount}" VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <!-- Right: Details and subject assignments -->
                <Border Grid.Column="1" Style="{ThemeResource CardBorderStyle}">
                    <StackPanel Spacing="12">
                        <TextBlock x:Name="SelectedClassTitle" Style="{ThemeResource SectionTitleStyle}" Text="Select a class" />

                        <TextBlock Style="{ThemeResource MutedTextStyle}" Text="Assigned Subjects" />
                        <ListView x:Name="AssignedSubjectsList" ItemsSource="{Binding SubjectsForClass}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SimpleLookup" xmlns:models="using:AutoTable.Models">
                                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="8,6">
                                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
                                        <Button Content="Remove" Click="RemoveSubject_Click" HorizontalAlignment="Right" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox x:Name="SubjectPicker" Width="200" ItemsSource="{Binding AllSubjects}" DisplayMemberPath="Name" SelectedIndex="0" />
                            <Button Content="Assign Subject" Click="AssignSubject_Click" Style="{ThemeResource SecondaryButtonStyle}" />
                        </StackPanel>

                        <TextBlock Style="{ThemeResource MutedTextStyle}" Text="Streams" />
                        <ListView x:Name="AssignedStreamsList" ItemsSource="{Binding StreamsForClass}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SimpleLookup" xmlns:models="using:AutoTable.Models">
                                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="8,6">
                                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
                                        <Button Content="Remove" Click="RemoveStream_Click" HorizontalAlignment="Right" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <ComboBox x:Name="StreamPicker" Width="200" ItemsSource="{Binding AllStreams}" DisplayMemberPath="Name" SelectedIndex="0" />
                            <Button Content="Assign Stream" Click="AssignStream_Click" Style="{ThemeResource SecondaryButtonStyle}" />
                        </StackPanel>

                        <Border Style="{ThemeResource TableHeaderRowStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <TextBox x:Name="NewStreamName" PlaceholderText="New stream name" Width="200" />
                                <Button Content="Create Stream" Click="CreateStream_Click" Style="{ThemeResource SecondaryButtonStyle}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{ThemeResource TableHeaderRowStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <TextBox x:Name="NewSubjectName" PlaceholderText="New subject name" Width="200" />
                                <Button Content="Create Subject" Click="CreateSubject_Click" Style="{ThemeResource SecondaryButtonStyle}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>

<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoTable.Views.ModerationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource SurfaceGrayBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16">

            <!-- FILTER BAR -->
            <Border Style="{StaticResource FilterBarStyle}">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" Header="Class" MinWidth="100"
                              ItemsSource="{Binding Classes}"
                              SelectedItem="{Binding SelectedClass, Mode=TwoWay}" />
                    <ComboBox Grid.Column="1" Header="Subject" MinWidth="160"
                              ItemsSource="{Binding Subjects}"
                              SelectedItem="{Binding SelectedSubject, Mode=TwoWay}" />
                    <Button Grid.Column="3" VerticalAlignment="Bottom"
                            Content="Load" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding LoadCommand}" />
                </Grid>
            </Border>

            <!-- STATUS STRIP -->
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="150">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Pending Review" />
                        <TextBlock Style="{StaticResource KpiValueStyle}"
                                   Foreground="{StaticResource WarningAmberBrush}"
                                   Text="{Binding PendingCount}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="150">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Approved" />
                        <TextBlock Style="{StaticResource KpiValueStyle}"
                                   Foreground="{StaticResource SuccessGreenBrush}"
                                   Text="{Binding ApprovedCount}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" MinWidth="150">
                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource MutedTextStyle}" Text="Rejected" />
                        <TextBlock Style="{StaticResource KpiValueStyle}"
                                   Foreground="{StaticResource DangerRedBrush}"
                                   Text="{Binding RejectedCount}" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- MODERATION TABLE -->
            <Border Style="{StaticResource TableCardStyle}">
                <StackPanel>
                    <Border Style="{StaticResource CardHeaderBorderStyle}">
                        <Grid>
                            <TextBlock Style="{StaticResource SectionTitleStyle}"
                                       Text="Marks Pending Moderation" VerticalAlignment="Center" />
                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                                <Button Content="Approve All" Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding ApproveAllCommand}" />
                                <Button Content="Publish Results" Style="{StaticResource DangerButtonStyle}"
                                        Command="{Binding PublishCommand}" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource TableHeaderRowStyle}">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="120" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Style="{StaticResource TableHeaderStyle}" Text="ASSESSMENT" />
                            <TextBlock Grid.Column="1" Style="{StaticResource TableHeaderStyle}" Text="CLASS" />
                            <TextBlock Grid.Column="2" Style="{StaticResource TableHeaderStyle}" Text="SUBJECT" />
                            <TextBlock Grid.Column="3" Style="{StaticResource TableHeaderStyle}" Text="ENTRIES" />
                            <TextBlock Grid.Column="4" Style="{StaticResource TableHeaderStyle}" Text="SUBMITTED BY" />
                            <TextBlock Grid.Column="5" Style="{StaticResource TableHeaderStyle}" Text="STATUS" />
                            <TextBlock Grid.Column="6" Style="{StaticResource TableHeaderStyle}" Text="ACTIONS" />
                        </Grid>
                    </Border>

                    <ListView ItemsSource="{Binding ModerationItems}" SelectionMode="None">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="MinHeight" Value="0" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnSpacing="12" Padding="16,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding AssessmentName}" FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="1" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding ClassName}" />
                                    <TextBlock Grid.Column="2" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding Subject}" />
                                    <TextBlock Grid.Column="3" Style="{StaticResource NumericCellStyle}"
                                               Text="{Binding EntryCount}" />
                                    <TextBlock Grid.Column="4" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding SubmittedBy}" />
                                    <TextBlock Grid.Column="5" Style="{StaticResource TableCellStyle}"
                                               Text="{Binding Status}" />
                                    <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="6">
                                        <Button Content="Approve" Style="{StaticResource PrimaryButtonStyle}"
                                                Padding="8,4" FontSize="12" />
                                        <Button Content="Reject" Style="{StaticResource DangerButtonStyle}"
                                                Padding="8,4" FontSize="12" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>

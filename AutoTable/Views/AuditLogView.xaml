<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AutoTable.Views.AuditLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource SurfaceGrayBrush}">
        <Grid Padding="16" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Style="{ThemeResource SectionTitleStyle}" Text="Termination / Audit Log" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Style="{ThemeResource PrimaryButtonStyle}" />
        </Grid>

        <TextBox x:Name="SearchBox" Grid.Row="1" PlaceholderText="Search by student name or reason" TextChanged="SearchBox_TextChanged" />

        <ListView x:Name="LogsList" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Spacing="4" Padding="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{Binding StudentName}" FontWeight="SemiBold" Width="220" />
                            <TextBlock Text="{Binding Reason}" Foreground="{ThemeResource TextSecondaryBrush}" Width="160" />
                            <TextBlock Text="{Binding TerminationDateDisplay}" Width="150" />
                            <TextBlock Text="{Binding LoggedAtDisplay}" />
                            <TextBlock Text="{Binding AnonymizedDisplay}" Foreground="{ThemeResource AccentPurpleBrush}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ListView x:Name="EnrollmentsList" Grid.Row="4" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Spacing="4" Padding="8">
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" Width="200" />
                            <TextBlock Text="{Binding LIN}" Foreground="{ThemeResource TextSecondaryBrush}" Width="120" />
                            <TextBlock Text="{Binding GuardianName}" Width="180" />
                            <TextBlock Text="{Binding GuardianPhone}" Width="140" />
                            <TextBlock Text="{Binding Status}" Foreground="{ThemeResource AccentPurpleBrush}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}" Style="{ThemeResource MutedTextStyle}" />
    </Grid>
</Page>